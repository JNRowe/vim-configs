#! /bin/sh

set -eu

gup --always

gup -u tags/libc.ctags \
    $(for d in /usr/share/lua/[0-9]*; do echo tags/lua${d##*/}.ctags; done) \
    $(for d in /usr/lib/ruby/[0-9]*; do echo tags/ruby${d##*/}.ctags; done)

gup -u $(ls -1 *.rst | sed 's,\.rst$,.html,')
